kind: pipeline
type: docker
name: default

steps:
  - name: test
    image: golang:1.14
    commands:
      - cd pkg/apikey
      - go test
      - cd ../filesid
      - go test
      - cd parser
      - go test
